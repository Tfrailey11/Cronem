[project]
name = "cronem"
version = "0.1.0"
dependencies = [
	"kernel",
	"python-dotenv"
]

[project.scripts]
Cronem = "cronem.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"



